services:
  mariadb:
    image: mariadb:${MARIADB_VERSION}
    container_name: mariadb
    restart: unless-stopped

    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${MARIADB_DATABASE}
      MARIADB_USER: ${MARIADB_USER}
      MARIADB_PASSWORD: ${MARIADB_PASSWORD}

    ports:
      - "${MARIADB_PORT}:3306"

    volumes:
      - mariadb_data:/var/lib/mysql
      - ./init:/docker-entrypoint-initdb.d

  adminer:
    image: adminer
    restart: always
    depends_on:
      - mariadb
    ports:
      - "${ADMINER_PORT}:8080"

volumes:
  mariadb_data:

  # web: 
  #   image: hoanglinhwebnuxt:latest
  #   restart: always
  #   ports:
  #     - 3000:3000
  #   depends_on:
  #     - db
  #   environment:
  #     DB_HOST: db
  #     DB_USER: root
  #     DB_PASSWORD: hoanglinh@132436
  #     DB_NAME: testdb
